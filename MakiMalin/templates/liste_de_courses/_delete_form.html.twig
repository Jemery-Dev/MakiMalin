<form method="post" action="{{ path('app_liste_de_courses_delete', {'id': liste_de_course.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_de_course.id) }}">
    <button class="btn">Delete</button>
</form>
